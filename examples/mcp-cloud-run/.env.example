# Environment Variables for MCP Server Cloud Run Deployment

# ===== GOOGLE CLOUD CONFIGURATION =====
GCP_PROJECT=your-project-id
GCP_REGION=us-central1

# ===== SERVICE MODE =====
# Options: "public" (with OAuth) or "private" (with IAM)
SERVICE_MODE=public

# ===== OAUTH CONFIGURATION (for public mode) =====
# Get these from Google Cloud Console > APIs & Services > Credentials
FASTMCP_SERVER_AUTH=fastmcp.server.auth.providers.google.GoogleProvider
FASTMCP_SERVER_AUTH_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
FASTMCP_SERVER_AUTH_GOOGLE_CLIENT_SECRET=your-client-secret
# This will be set automatically after deployment
FASTMCP_SERVER_AUTH_GOOGLE_BASE_URL=https://your-service-xyz-uc.a.run.app
# Optional: Restrict to specific domains (comma-separated)
FASTMCP_SERVER_AUTH_GOOGLE_ALLOWED_DOMAINS=yourdomain.com,anotherdomain.com
# Optional: Custom redirect path (default: /auth/callback)
FASTMCP_SERVER_AUTH_GOOGLE_REDIRECT_PATH=/auth/callback

# ===== IAM CONFIGURATION (for private mode) =====
# Comma-separated list of user emails to grant access
AUTHORIZED_USERS=user1@domain.com,user2@domain.com
# Comma-separated list of service accounts to grant access
AUTHORIZED_SERVICE_ACCOUNTS=service-account@project.iam.gserviceaccount.com

# ===== VAC CONFIGURATION =====
VAC_CONFIG_FOLDER=/app/config
LOG_LEVEL=INFO

# ===== MCP SERVER SETTINGS =====
MCP_SERVER_NAME=sunholo-vac-server
MCP_INCLUDE_VAC_TOOLS=true

# ===== CORS CONFIGURATION =====
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://claude.ai,https://yourdomain.com

# ===== CLOUD RUN SETTINGS =====
# Port is automatically set by Cloud Run, but you can override for local testing
PORT=8080

# ===== OPTIONAL: MONITORING =====
# Google Cloud Monitoring
ENABLE_MONITORING=true
# Sentry DSN for error tracking
SENTRY_DSN=

# ===== OPTIONAL: RATE LIMITING =====
# Rate limit per minute per IP
RATE_LIMIT=100
# Rate limit burst size
RATE_LIMIT_BURST=20